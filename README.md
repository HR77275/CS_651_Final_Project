# 651 Project

Initial Pascal VOC 2012 semantic segmentation setup.

## Next steps

1. Create a virtual environment and install `requirements.txt`.
2. Run `python scripts/check_voc_dataloader.py --download` once dependencies are installed.
3. Use the resulting dataloader as the base for training experiments.
